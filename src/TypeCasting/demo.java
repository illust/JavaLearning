package TypeCasting;

/**
 * Created by Sven on 2018/2/7.
 *
 */

/**
 *  类型转换的概念：
 *  - 又称为塑型（type-casting)
 *  - 转换方式
 *    1. 隐式的类型转换
 *    2. 显式的类型转换
 *  - 转换的方向
 *    1. 向上转型
 *    2. 向下转型
 *  类型转换规则：
 *   - 基本类型之间的转换
 *     1. 将值从一种类型转换成另一种类型
 *   - 引用变量的类型转换
 *     1. 将引用转换为另一类型的引用，并不改变对象本身的类型
 *     2. 只能被转为
 *       1）任何一个（直接或间接）超类的类型（向上转型）
 *       2）对象所属的类（或其超类）实现的一个接口（向上转型）
 *       3）被转换为引用指向的对象的类型（唯一可以向下转型的情况）
 */
public class demo {
}
